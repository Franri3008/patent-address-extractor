[project]
name = "patent-address-extractor"
version = "0.1.0"
description = "Pipeline for extracting inventor and applicant addresses from WO patents."
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
