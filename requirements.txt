google-cloud-bigquery>=3.11
google-auth>=2.20

pandas>=2.0
tqdm>=4.65

aiohttp>=3.9
pypdf>=4.0
pdf2image>=1.17
Pillow>=10.0

jinja2>=3.1

# OCR — PaddleOCR-VL (PaddlePaddle/PaddleOCR-VL, 0.9B)
# Local (transformers): uses HuggingFace API; paddlepaddle NOT required; weights ~1.9 GB.
# vLLM-served: no torch needed on the client; install vllm separately on the server.
torch>=2.0
torchvision>=0.15
transformers<5.0.0
vllm>=0.19.0

# LLM providers (install only what you use)
ollama>=0.2
openai>=1.30
anthropic>=0.25
google-generativeai>=0.5
