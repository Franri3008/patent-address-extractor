You are extracting structured address information from OCR text of a WIPO PCT patent.

The patent uses standard WIPO section numbering (sections are usually in increasing order, but not always):
  - (71) = Applicant(s)  — company or individual + address
  - (72) = Inventor(s)   — format: "LASTNAME, Firstname; street, city, region (CC)."
  - (74) = Agent / Law firm — name + address

Important rules:
  1. Extract addresses EXACTLY as they appear in the text. Do NOT translate, infer, normalise, or reformat.
  2. The text may be in ANY language. Preserve the original language of addresses entirely.
  3. If an address is not found for an entity, set "address" to null.
  4. Multiple inventors / applicants are each a separate object in their list.
  5. Return ONLY valid JSON — no explanation, no markdown fences, no preamble.
  6. If the (72) section is absent or the text clearly does not contain inventor data, set "found" to false.
  7. Sections may appear out of order (e.g. (72) before (71)). Parse by section number, not by position.
  8. Sometimes a section cross-references another instead of listing its own data. For example, the inventor section (72) may say just "Inventor; and" or "Inventeur; et" followed immediately by (71), meaning the inventor is the same person(s) listed in (71). In such cases, copy the inventor data from the applicant section. The same can happen in reverse.
  9. A single section (e.g. (71)) may contain multiple entities separated by a period, semicolon, or new line. Each entity is a separate object — do not merge them into one.
{% if known_applicants or known_inventors %}

Known entities from the patent registry (use these to ensure all are included in the output — find their addresses in the text):
{% if known_applicants %}  Applicants: {{ known_applicants | join(" | ") }}{% endif %}
{% if known_inventors %}  Inventors: {{ known_inventors | join(" | ") }}{% endif %}
  If an entity from this list appears in the text (even with slight OCR misspellings), include it. Set "address" to null if no address is found for it.
{% endif %}

OCR TEXT (may span multiple pages, from a two-column layout):
---
{{ ocr_text }}
---

Return this exact JSON structure. Deduplicate addresses: assign each unique address an integer id, list all addresses in the "addresses" array, and reference them by address_id in entities. If two entities share the same address, they must reference the same id.
{
  "addresses": [
    {"id": 1, "address": "650 Anlaby Road, Kingston Upon Hull Yorkshire HU3 6UU (GB)"},
    {"id": 2, "address": null}
  ],
  "entities": {
    "inventors":  [{"name": "RYAN, David", "address_id": 1}],
    "applicants": [{"name": "HABITAT AQUATICS LTD", "address_id": 1}],
    "agents":     [{"name": "Some Agent", "address_id": 2}]
  },
  "found": true
}
